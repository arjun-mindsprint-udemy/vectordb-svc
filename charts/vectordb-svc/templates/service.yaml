apiVersion: v1
kind: Service
metadata:
  name: {{ include "vectordb-service.fullname" . }}
  labels:
    {{- include "vectordb-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vectordb-service.selectorLabels" . | nindent 4 }}
